# -*- coding: utf-8 -*-
"""
shared 包 -- 通用共享模块
===========================

本包提供所有模块共用的基础设施组件，包括环境配置、数据库连接和日志配置。

模块清单
--------
env     AppCfg 类：从 .env 文件加载环境变量，统一管理 API 密钥、
        Telegram 配置、数据库凭据等所有运行时配置。
db      asyncpg 连接池管理：创建和管理到 TimescaleDB 的连接池，
        提供 get_pool() 和 close_pool() 函数。
log     日志配置：setup_logging() 函数，统一日志格式、级别、输出目标。

设计原则
--------
- 所有模块通过 shared 包获取配置，避免硬编码和重复读取环境变量。
- 连接池由 db 模块统一管理，避免多个模块各自创建连接。
- 日志格式统一为 ISO8601 时间戳 + 级别 + 模块名，便于 grep 分析。

作者: nt-base system
版本: 2.0.0
"""
